---
description: Swedish SVT Text — page, search, topic, index.
argument-hint: "[page|index|search <q>|topic <name>|topics|refresh]"
allowed-tools: ["Bash"]
---

Run the teletext CLI for SVT Text (Sveriges Television, code `svt`) with the user's arguments and show its output verbatim.

!`node "${CLAUDE_PLUGIN_ROOT}/dist/cli.js" --provider=svt $ARGUMENTS`
